---
topic: collections
block: "I. Типы данных и модель объекта"
number: 8
status: todo
---

# Собес Python — collections

## БАЗА

1. Что даёт `collections.deque` по сравнению с `list`? Какая сложность у `append`, `appendleft`, `pop`, `popleft` в каждом из них?
2. Какая сложность у `deque[i]` для произвольного `i`? Почему `deque` не подходит как замена `list` в задачах со случайным доступом?
3. Что делает параметр `maxlen` у `deque`? Что происходит при `append` в полную очередь и на какой стороне?
4. Чем `Counter` отличается от `defaultdict(int)`? Что вернёт `Counter()['missing']` и что вернёт `defaultdict(int)['missing']` — и чем эти два поведения различаются по побочному эффекту?
5. Как работает `defaultdict`: в какой момент вызывается `default_factory` и что произойдёт при `d['x']`, если `default_factory is None`?
6. Что вернёт `Counter('aabbbc').most_common(2)` и по какому правилу упорядочены элементы с равным счётчиком?
7. Чем `Counter.update()` отличается от `dict.update()`? Что делает `Counter.subtract()` и чем отличается от оператора `-`?
8. Зачем нужен `OrderedDict`, если с Python 3.7 обычный `dict` сохраняет порядок вставки? Назови минимум три отличия.
9. Что делает `OrderedDict.move_to_end(key, last=False)` и `popitem(last=False)`? Есть ли аналог у `dict`?
10. Что такое `ChainMap` и как в нём разрешается поиск ключа? В какой словарь попадёт запись при `cm['x'] = 1`?
11. Как ведёт себя `del cm['x']` в `ChainMap`, если `x` есть только во втором словаре цепочки?
12. Чем `namedtuple` отличается от обычного кортежа по памяти и по API? Что делают `_replace`, `_asdict`, `_fields`?

## СРЕДНИЙ УРОВЕНЬ

13. Как `deque` устроен внутри CPython? Что такое `block` в `dequeobject`, сколько элементов в блоке и почему выбрано именно такое число?
14. Почему `deque.rotate(n)` для больших `n` работает быстрее, чем `n` последовательных `rotate(1)`? Какая у него сложность?
15. Что вернёт `Counter(a=3, b=-1) + Counter(a=-3, b=1)` и почему? Какое правило применяют арифметические операторы `Counter` к нулям и отрицательным значениям?
16. Что делают унарные `+c` и `-c` для `Counter`? Зачем они существуют?
17. `Counter.total()` — с какой версии Python, чем отличался идиоматичный способ до неё и почему добавили отдельный метод?
18. `defaultdict(list)` внутри рекурсивной функции: как сделать бесконечно вложенный `defaultdict` одной строкой и в чём опасность такой структуры при чтении?
19. Почему `defaultdict.__missing__` вызывается только из `__getitem__`, но не из `.get()` и не из `in`? Где это ломает код?
20. Как `OrderedDict` реализован в CPython 3.x: какая структура данных отвечает за порядок и почему `OrderedDict` тяжелее по памяти, чем `dict`?
21. `od1 == od2` для двух `OrderedDict` и `od == d` для `OrderedDict` vs `dict` — в каких случаях результат разный?
22. Почему `functools.lru_cache` внутри использует не `OrderedDict`, а самодельный кольцевой двусвязный список? Что это дало?
23. `ChainMap.new_child()` vs `ChainMap(d, *cm.maps)` — в чём разница? Как реализуются вложенные scope-ы через `ChainMap` и где реальный пример в stdlib/`argparse`?
24. Что вернёт `len(ChainMap({'a':1}, {'a':2, 'b':3}))` и какая сложность у `len`, `iter`, `__contains__` для цепочки из N словарей?
25. `namedtuple` генерирует класс через `exec` из шаблона строки. Зачем так сделано и какова цена при импорте модуля с сотней namedtuple?
26. `typing.NamedTuple` vs `collections.namedtuple` vs `@dataclass(frozen=True, slots=True)` — сравни по памяти, скорости создания и распаковке.
27. Зачем нужен `collections.UserDict`, если можно наследовать `dict`? Приведи конкретный метод, который сломается при наследовании от `dict`.
28. `collections.abc` vs конкретные классы: что проверяет `isinstance(x, collections.abc.Sequence)` и почему `str` — `Sequence`, а `dict` — нет? Почему `deque` не был `Sequence` до какого-то момента?

## ЗАДАЧИ (напиши код)

29. Реализуй LRU-кэш фиксированного размера на `OrderedDict` (`get`/`put` за O(1)). Затем перепиши на `dict` без `OrderedDict` — что теряется? Какая сложность в обоих?
30. Скользящее окно максимума: дан список и ширина `k`, верни максимум каждого окна за O(n) с помощью `deque`. Объясни, что хранится в `deque` — значения или индексы, и почему.
31. Напиши функцию, группирующую список записей `(key, value)` в `dict[key, list[value]]` тремя способами: `defaultdict`, `dict.setdefault`, `itertools.groupby`. Сравни сложность и поведение на неотсортированных данных.
32. Реализуй частотный анализ большого текстового потока: топ-K слов при ограничении памяти. `Counter.most_common(K)` — какая у него сложность и что он использует внутри? Предложи вариант без хранения всех уникальных слов.
33. Напиши `deque`-based буфер последних N событий с O(1) вставкой и `to_list()`. Что произойдёт при итерации по `deque` во время `append` из другого потока?
34. Реализуй разрешение конфигов: defaults → файл → env → CLI через `ChainMap`. Добавь метод `flatten()` в обычный `dict`. Какой порядок словарей в `maps` нужен и почему легко ошибиться?

---

# 20 ВОПРОСОВ НА СЕНЬОРНОСТЬ

**35.** `deque` в CPython — двусвязный список блоков по 64 указателя (`BLOCKLEN`). Почему не 32 и не 128? Как размер блока связан с cache line и с `sizeof(block)` в 512 байт?

**36.** `deque` держит `freelist` из блоков (`MAXFREEBLOCKS = 16`). Что будет с производительностью паттерна «наполнить 1M → полностью опустошить → снова наполнить», и как это отличается от `list`, который память под `ob_item` не отдаёт до `list_clear`?

**37.** `deque.rotate(n)` реализован в `_deque_rotate` без копирования элементов — он двигает указатели `leftblock`/`rightblock`. Почему тогда `d.rotate(len(d)//2)` не является O(1)? Где именно уходит время?

**38.** Итерация по `deque` бросает `RuntimeError: deque mutated during iteration`. Какое поле `dequeobject` это детектит и почему аналогичная защита у `list` отсутствует — там просто получаешь тихо неверный результат?

**39.** `deque.append`/`popleft` называют thread-safe. Что конкретно даёт эту гарантию — GIL или что-то в реализации? Что происходит с этой гарантией в free-threaded CPython (PEP 703) и что там добавили в `_collectionsmodule.c`?

**40.** `Counter(long_iterable)` идёт по fast path в `_count_elements` — C-функция из `_collections`. При каком условии CPython откатывается на медленный Python-цикл, и во сколько раз падает скорость? Что именно проверяет `_count_elements` перед тем, как использовать `PyDict_SetItem` напрямую?

**41.** `Counter.most_common(k)` для `k < n` использует `heapq.nlargest` — O(n log k), а для `k=None` — `sorted` O(n log n). При каком отношении k/n граница выгодности переворачивается и почему в реальности `nlargest` проигрывает `sorted` раньше теоретической точки?

**42.** `Counter` наследуется от `dict`. Почему `Counter.fromkeys` намеренно поднимает `NotImplementedError`? Какая семантическая ловушка была бы, если бы его оставили унаследованным?

**43.** `c = Counter(); c['x'] += 1` — сколько раз вызывается `__missing__` и сколько хеш-lookup'ов происходит? Сравни с `d = defaultdict(int); d['x'] += 1` по числу словарных операций в байткоде (`dis` в помощь).

**44.** Приложение считает частоты через `Counter` на пользовательском вводе. Как это превращается в DoS-вектор? Причём тут hash randomization (PEP 456, SipHash) и почему она защищает от коллизионной атаки, но не от роста памяти?

**45.** До Python 3.5 `OrderedDict` был чистым Python; в 3.5 появилась C-реализация (`odictobject.c`). Что она сделала с `__sizeof__` — почему `OrderedDict` всё ещё в ~2 раза тяжелее `dict` того же размера? Куда уходят байты?

**46.** `dict` с 3.7 гарантирует порядок вставки (PEP 468 / compact dict Раймонда Хеттингера). Почему `OrderedDict` не удалили из stdlib? Назови три сценария, где замена на `dict` — регрессия, а не апгрейд.

**47.** `OrderedDict.popitem(last=False)` — O(1). Как compact `dict` устроен внутри (`dk_entries` + `dk_indices`), и почему `dict` не может дать O(1) на удаление с начала без tombstone-мусора? Что происходит при паттерне FIFO на обычном `dict` с 10M операций?

**48.** `move_to_end` на C-`OrderedDict` инвалидирует итераторы. Как именно детектится мутация в `odictobject.c` и почему в чистом Python-варианте до 3.5 та же операция могла тихо пропустить элемент?

**49.** `ChainMap.__contains__` — линейный проход по `maps`. Ты строишь систему шаблонов с 200 вложенными scope-ами и миллионом lookup'ов. Где ломается и что делаешь вместо: flatten на каждом уровне, кэш, или что-то третье? Обоснуй trade-off по памяти vs invalidation.

**50.** `ChainMap` не копирует словари, он держит ссылки. Изменение исходного `dict` видно в `ChainMap` сразу. Придумай баг, который из этого рождается в конфиг-системе с `new_child()` в цикле, и объясни, почему `copy.copy(cm)` его не чинит.

**51.** `namedtuple` в py2 не имел `__slots__` в шаблоне до определённого момента; сейчас имеет. Что дают `__slots__ = ()` в сгенерированном классе и сколько байт экономится на инстанс на 64-битной платформе? Почему `_fields` — атрибут класса, а не свойство?

**52.** `namedtuple` строит `__new__` через `exec(template)` с `_tuplegetter` (C, с 3.8) вместо `property(itemgetter(i))`. Что дал `_tuplegetter` по скорости доступа к полю и почему `itemgetter` был медленнее, хотя тоже C?

**53.** У тебя 500M событий `(user_id, ts, event_type)` и 16GB RAM, надо посчитать топ-1000 event_type на пользователя. `defaultdict(Counter)` — сколько это займёт и почему ляжет? Опиши архитектуру: что вместо, как шардить, где `deque(maxlen=)` уместен, а где нет.

**54.** В Java есть `ArrayDeque` (кольцевой буфер на массиве), в Python — блочный связный список. В Go `container/list` вообще не используют в горячем коде, предпочитая слайсы. Почему CPython выбрал блоки, а не кольцевой буфер, и что бы сломалось при переходе на `ArrayDeque`-подход?

---

**Бонус-киллер:** `Counter({'a': 1}) == Counter({'a': 1, 'b': 0})` вернёт `False`, при этом `Counter({'a':1, 'b':0})['b']` вернёт `0` — ровно то же, что вернёт первый счётчик для `'b'`. Два объекта неотличимы по любому запросу значения, но не равны. Это баг равенства, баг модели `Counter`, или единственно возможное поведение — и что тогда должен возвращать `hash`?

Хочешь разбор ответов? Скажи номера.
