---
topic: threading
block: "IV. Конкурентность"
number: 29
status: todo
---

# Собес Python — THREADING

## БАЗА

1. Чем `threading.Lock` отличается от `threading.RLock`? Какой код сломается на `Lock`, но пройдёт на `RLock`?

2. Что делает `Lock.acquire(blocking=False)` и что возвращает? Как отличить успех от неудачи?

3. Почему `with lock:` предпочтительнее пары `acquire()`/`release()`? Что происходит при исключении внутри критической секции?

4. Может ли `Lock.release()` вызвать поток, который его не захватывал? А `RLock.release()`? Что будет?

5. `threading.Semaphore(3)` — что означает аргумент? Чем `BoundedSemaphore` отличается от `Semaphore`?

6. Опиши жизненный цикл `threading.Event`: `set`, `clear`, `wait`, `is_set`. Что вернёт `wait(timeout=1)` по таймауту?

7. Зачем `Condition` нужен связанный с ним lock? Что делает `wait()` с этим lock'ом?

8. Разница `notify()` и `notify_all()` у `Condition`. Когда `notify()` приводит к зависанию?

9. Что такое deadlock? Приведи минимальный пример на двух `Lock` и двух потоках.

10. `thread.daemon = True` — что меняется при завершении процесса? Когда флаг должен быть выставлен?

11. Чем `join()` отличается от `join(timeout=5)`? Как узнать, что поток на самом деле завершился?

12. Что вернёт `threading.current_thread()` в главном потоке? Зачем нужен `threading.local()`?

## СРЕДНИЙ УРОВЕНЬ

13. `Lock` в CPython — это обёртка над чем на уровне ОС? Чем реализация на Linux отличается от Windows?

14. Почему `RLock` в Python 3.13 медленнее чистого `Lock`? Какое поле объекта он обязан читать на каждом `acquire`?

15. `x += 1` для int из двух потоков — почему теряются инкременты? Разбери через `dis`, где именно разрывается атомарность.

16. Какие операции над `list`/`dict` можно считать атомарными и почему? Где именно проходит граница и что её задаёт?

17. Что произошло с convoy effect до Python 3.2 и что изменил новый GIL Антуана Питру? При чём тут `sys.setswitchinterval`?

18. Почему `queue.Queue` не требует внешней блокировки? Какие примитивы у него внутри и зачем их несколько?

19. `Condition.wait()` без цикла `while` — какая ошибка? Что такое spurious wakeup и бывает ли он в CPython?

20. Как работает `Barrier`? Что такое `BrokenBarrierError` и как барьер попадает в broken-состояние?

21. Почему нельзя убить поток извне? Какие полулегальные способы существуют и чем каждый плох?

22. `threading.Timer` — на чём построен внутри? Почему `cancel()` не гарантирует отмену?

23. Что делает `sys.setswitchinterval(0.0001)`? Как это влияет на throughput CPU-bound потоков и на latency I/O-потоков?

24. Разница между `threading.Event` и `Condition` для сигнализации one-shot и many-shot. Когда `Event` даёт потерю сигнала?

25. Что такое lock ordering и почему он решает deadlock? Как его нарушает `sorted()` по `id()` объектов?

26. Почему `time.sleep()` отпускает GIL, а тяжёлый `re.match` — нет? Как это узнать для произвольной C-функции?

27. `threading.stack_size(N)` — на что влияет и какие ограничения по платформам? Когда приходится трогать?

## ЗАДАЧИ (напиши код)

28. Реализуй потокобезопасный счётчик с методами `inc()`, `dec()`, `value`. Сделай версию на `Lock` и версию без блокировок вообще — объясни, за счёт чего вторая корректна.

29. Напиши bounded blocking queue (`put`/`get` с ограничением размера) на `Condition`, не используя `queue.Queue`. Обработай таймаут в `get`. Какая сложность и где узкое место?

30. Реализуй `ReadWriteLock`: много читателей или один писатель. Сначала writer-preference версию, потом объясни, где в твоём коде читатели могут заморить писателя голодом.

31. Напиши декоратор `@once`, гарантирующий, что функция выполнится ровно один раз при вызове из N потоков, а остальные дождутся результата и получат его же. Исключение внутри — что делаешь?

32. Реализуй пул из 5 воркеров, забирающих задачи из очереди, с graceful shutdown по `Event`. Как гарантировать, что ни одна задача не потеряется?

33. Напиши функцию, которая детектит deadlock двух потоков на двух `Lock`: собери минимальный воспроизводимый пример, потом почини его через `acquire(timeout=)` с бэкоффом. Почему просто timeout — не решение?

---

# 20 ВОПРОСОВ НА СЕНЬОРНОСТЬ

**34.** До Python 3.2 GIL переключался каждые 100 байткодов. Питру заменил это на 5 мс интервал + `gil_drop_request`. Почему счётчик байткодов давал convoy effect и почему поток, отпустивший GIL, чаще всего забирал его обратно?

**35.** PEP 703 (free-threaded CPython, `--disable-gil`). Что такое biased reference counting и deferred reference counting, зачем понадобились оба? Какую цену платит однопоточный код?

**36.** В free-threaded сборке 3.13 `dict` и `list` защищены per-object lock'ами. Что такое critical sections в этой реализации и почему они умеют reentrant-освобождаться при блокировке?

**37.** `threading.Lock` в CPython 3.13+ — это `PyMutex`, 1 байт вместо `pthread_mutex_t`. Как в один байт помещаются locked-бит и waiters-флаг, и на чём построено ожидание?

**38.** Почему `list.append` атомарен, а `list[i] += 1` — нет, хотя оба заканчиваются одним байткодом-мутатором? Разбери через `dis` и через момент проверки `gil_drop_request` в `_PyEval_EvalFrameDefault`.

**39.** В Java `synchronized` умеет biased locking и escape analysis выкидывает блокировки вообще. В Python — нет. Что в объектной модели CPython делает такую оптимизацию невозможной?

**40.** Go детектит deadlock рантаймом («all goroutines are asleep»). Почему CPython не может сделать то же самое, хотя тоже владеет всеми примитивами?

**41.** Ты захватил `Lock` и получил `KeyboardInterrupt` между `acquire()` и `try:`. Покажи окно гонки. Как `with` его закрывает и что решил PEP 343 в этой части?

**42.** Сигналы в Python доставляются только в главном потоке. Почему? Что произойдёт с `signal.signal` из рабочего потока и как `wakeup_fd` обходит проблему?

**43.** `fork()` в процессе с захваченными lock'ами — классический deadlock в потомке. Что делает `os.register_at_fork` и почему `logging` до 3.7 регулярно вешал форкнутые процессы?

**44.** `Condition.wait()` реализован через создание нового `Lock` на каждый вызов и его `acquire()`. Зачем именно новый объект каждый раз, а не переиспользование?

**45.** Есть счётчик под `Lock`: 8 потоков, 10M инкрементов. С `Lock` — 12 с, без — 0.9 с и неверный результат, с `itertools.count()` — 1.4 с и верный. Объясни все три числа.

**46.** `Semaphore` в CPython реализован через `Condition`, а не через POSIX `sem_t`. Какую цену это даёт по числу системных вызовов и почему выбрали так?

**47.** Timing attack на сравнение токенов в многопоточном сервере. Как переключение GIL каждые 5 мс одновременно и маскирует, и усиливает утечку по времени? Что даёт `hmac.compare_digest`?

**48.** Thread pool на 200 потоков против 200 корутин: посчитай RSS для каждого. Откуда берётся 8 МБ на поток и почему реальное потребление меньше? Где ломается сравнение?

**49.** Сервис держит 500 идущих потоков, каждый — блокирующий драйвер БД без async-аналога. RAM 16 ГБ, p99 latency критична. Твоя архитектура? Обоснуй выбор против процессов и против переписывания драйвера.

**50.** `threading.local()` — где физически лежат данные и что происходит с ними при смерти потока? Почему до 3.8 была утечка при комбинации `local` + циклические ссылки?

**51.** GIL атомарен для байткодов, но `sys.setswitchinterval` не гарантирует переключение. Приведи паттерн, где CPU-bound поток удерживает GIL сильно дольше 5 мс, и объясни почему.

**52.** Ты пишешь библиотеку и хочешь, чтобы она работала и с GIL, и без него (3.13t). Какие ваши инварианты, «бесплатные» сегодня, перестают быть верными? Как проверить в CI?

**53.** `RLock` хранит owner как thread id. Что произойдёт с этим id при переиспользовании pthread id ОС после смерти потока? Реальна ли угроза и что делает CPython, чтобы её не было?

---

**Бонус-киллер:** Два потока делают `with lock_a: with lock_b: ...` в одном и том же порядке — deadlock невозможен. Ты добавляешь третий поток, который берёт только `lock_b`, и внутри вызывает `logging.info()`. Deadlock вернулся. Как?

Хочешь разбор ответов? Скажи номера.
