---
topic: http-clients
block: "X. Прикладное"
number: 65
status: todo
---

# Собес Python — HTTP-клиенты

## БАЗА

1. `requests.get(url)` vs `requests.Session().get(url)` — что переиспользуется во втором случае и что теряется в первом?
2. Какой таймаут стоит по умолчанию в `requests`, если не передать `timeout=`? А в `httpx`? А в `aiohttp`?
3. `timeout=5` в `requests` — это таймаут на что именно? Что означает кортеж `timeout=(3, 10)`?
4. Чем `response.text` отличается от `response.content` и откуда берётся кодировка для `.text`?
5. `response.json()` — какое исключение прилетит на невалидном теле и какое на пустом ответе?
6. Что делает `raise_for_status()` и для каких диапазонов статусов он молчит?
7. `params=`, `data=`, `json=`, `files=` — какой `Content-Type` выставляется в каждом случае?
8. Разница между `allow_redirects=True/False`. Что лежит в `response.history` после трёх редиректов?
9. Почему `requests` — синхронный, `aiohttp` — только асинхронный, а `httpx` — оба? Что это меняет для вызывающего кода?
10. Как в `aiohttp` правильно создать и закрыть `ClientSession`? Что будет, если не закрыть?
11. `httpx.Client` vs `httpx.AsyncClient` — какие методы становятся корутинами и что происходит с закрытием?
12. Что такое connection pooling и какие параметры им управляют в `requests` (`HTTPAdapter`) и в `httpx` (`Limits`)?
13. Чем `stream=True` в `requests` отличается от обычного запроса? Что обязательно сделать после?
14. Какие 4 базовых класса исключений есть у `requests` (`RequestException` и наследники) и как отличить таймаут от отказа в соединении?

## СРЕДНИЙ УРОВЕНЬ

15. `requests` создаёт новый `Session` на каждый вызов `requests.get()`. Какая именно работа делается заново — TCP handshake, TLS handshake, DNS? Сколько это RTT?
16. `HTTPAdapter(pool_connections=10, pool_maxsize=10)` — что считает первый параметр и что второй? Что произойдёт при 50 потоках на одном `Session`?
17. Что печатается в лог при `pool_maxsize` меньше числа потоков — `Connection pool is full, discarding connection`. Что происходит с запросом: падает, ждёт или открывает новое соединение?
18. `Retry(total=3, backoff_factor=0.5)` из `urllib3` — по какой формуле считаются паузы и почему первая пауза равна нулю?
19. Какие HTTP-методы `urllib3.Retry` ретраит по умолчанию (`allowed_methods`)? Почему `POST` там нет и когда это правило безопасно нарушить?
20. `Retry(status_forcelist=[500, 502, 503, 504])` — почему без `raise_on_status` и `allowed_methods` этот конфиг часто не срабатывает так, как ожидают?
21. `respect_retry_after_header` — что делает `urllib3` с заголовком `Retry-After: 120` и почему это может подвесить твой воркер?
22. `httpx` не ретраит на уровне HTTP из коробки. Что умеет `HTTPTransport(retries=3)` и чем это принципиально уже, чем `urllib3.Retry`?
23. `httpx.Timeout(connect=5, read=10, write=5, pool=1)` — четыре разных таймаута. Что именно измеряет `pool` и в каком случае он стреляет?
24. В `aiohttp` есть `ClientTimeout(total, connect, sock_connect, sock_read)`. Чем `connect` отличается от `sock_connect`?
25. `requests` не имеет таймаута на общую длительность запроса. Как это ломается при медленной отдаче тела по байту в секунду и чем это лечится?
26. `aiohttp.TCPConnector(limit=100, limit_per_host=0)` — что означают нули и почему `limit_per_host` по умолчанию отключён?
27. Как в `aiohttp` работает `ttl_dns_cache` и почему дефолт в 10 секунд ломается при DNS-балансировке через короткий TTL?
28. `httpx` требует явного `follow_redirects=True`, `requests` редиректит по умолчанию. Какое поведение считается более правильным и какой класс багов ловит вариант `httpx`?
29. Что происходит с телом и методом при редиректе `301` на `POST` в `requests`? Что говорит RFC 7231 и что реально делают браузеры?
30. `Session.headers` vs `headers=` в вызове — как они мержатся? Как удалить заголовок, заданный на уровне сессии?
31. Почему `aiohttp.ClientSession` нельзя создавать вне работающего event loop и что именно ломается при глобальной сессии на уровне модуля?

## ЗАДАЧИ (напиши код)

32. Напиши функцию `fetch_with_retry(url, attempts=3)` на `requests` с экспоненциальным backoff и джиттером. Ретраить только на 5xx, 429 и таймаутах. Верни последний ответ или подними последнее исключение. Какая суммарная максимальная задержка?
33. Собери `requests.Session` с `HTTPAdapter` + `urllib3.Retry`: 3 ретрая, backoff 0.5, статусы 429/500/502/503/504, ретраи на `POST` тоже. Смонтируй на `http://` и `https://`.
34. Реализуй асинхронный `fetch_all(urls)` на `aiohttp`: одна сессия, не более 10 одновременных запросов, per-request таймаут 5 сек, ошибки не роняют остальные — верни список `(url, status | exception)`.
35. Напиши на `httpx.AsyncClient` кастомный transport-обёртку, который логирует метод, URL и длительность каждого запроса, включая ретраи. Не трогая код вызывающего.
36. Реализуй circuit breaker поверх `httpx.Client`: после 5 подряд ошибок открывается на 30 сек и сразу кидает `CircuitOpen`, потом half-open с одним пробным запросом. Потокобезопасно.
37. Напиши стриминговую загрузку большого файла на `requests` c докачкой через `Range:` при обрыве. Проверь, что сервер вернул `206`. Что делать, если сервер ответил `200`?
38. Реализуй rate limiter (token bucket, 10 req/s) для `aiohttp`, который делится между всеми корутинами одной сессии. Без busy-wait.

---

# 20 ВОПРОСОВ НА СЕНЬОРНОСТЬ

**39.** `requests.get()` без `Session` при 1000 запросов к одному хосту: измерь TLS handshake. Почему разница с сессией на HTTPS больше, чем на HTTP, и во сколько раз примерно?

**40.** `requests` до сих пор не умеет HTTP/2, `httpx` умеет через `h2`. Что даёт мультиплексирование при 100 параллельных запросах к одному хосту и в каком сценарии HTTP/2 окажется медленнее HTTP/1.1 с пулом?

**41.** HTTP/2 head-of-line blocking на уровне TCP — почему `httpx[http2]` на плохой сети может проиграть шести HTTP/1.1 соединениям? Что решает QUIC и почему в Python это до сих пор экзотика?

**42.** `urllib3.Retry.backoff_factor=0.5`, `total=5` — посчитай реальные паузы. Что такое `BACKOFF_MAX` и почему без джиттера 1000 клиентов, упавших одновременно, положат сервер повторно?

**43.** `Retry` ретраит `POST /payments` при `ConnectionError` на этапе чтения ответа. Платёж уже прошёл. Как выглядит идемпотентный дизайн через `Idempotency-Key` и почему это ответственность сервера, а не клиента?

**44.** `requests` в 20 потоках на одном `Session` — thread-safe ли `Session`? Что говорит документация, что говорят баг-репорты про `cookies` и `HTTPAdapter`, и где именно гонка?

**45.** `aiohttp.ClientSession`, созданный на уровне модуля, падает с `Timeout context manager should be used inside a task` или `attached to a different loop`. Что именно привязывается к loop внутри `TCPConnector` и почему `asyncio.run()` дважды это ломает?

**46.** `Unclosed client session` / `Unclosed connector` в `aiohttp` — где это печатается, почему в `__del__`, и почему сокеты при этом могут жить до GC? Как это выглядит под нагрузкой в проде?

**47.** У тебя 50k RPS исходящих HTTP к 3 бэкендам, Python. Сколько нужно соединений на воркер, как считаешь `limit_per_host`, и в какой момент упрёшься в ephemeral ports (`net.ipv4.ip_local_port_range`) и `TIME_WAIT`?

**48.** SSRF: `requests.get(user_url)` с включёнными редиректами. Как атака проходит через `301` на `http://169.254.169.254/`? Почему валидация URL до запроса не помогает и что такое DNS rebinding в этом контексте?

**49.** `verify=False` в `requests` — что именно отключается: проверка цепочки, проверка hostname, или оба? Что печатает `InsecureRequestWarning` и почему `verify='/path/ca.pem'` — не то же самое, что `REQUESTS_CA_BUNDLE`?

**50.** `requests` тащит собственный `certifi` bundle, а не системный trust store. Плюс это или минус в корпоративной сети с MITM-прокси? Как это решают `truststore` (PEP-совместимый) и `httpx`?

**51.** Заголовок `Authorization` при редиректе на другой хост: что делает `requests` (`rebuild_auth`), что делает `httpx`, что делает `curl`? Какая CVE-шная история за этим стоит?

**52.** `response.iter_content(chunk_size=8192)` vs `response.raw.read()` — что происходит с `Content-Encoding: gzip` в каждом случае? Почему `decode_content` по умолчанию `False` в `raw`?

**53.** Zip-бомба через HTTP: сервер отдаёт `Content-Encoding: gzip`, 1 МБ распаковывается в 10 ГБ. `response.content` съедает всю RAM. Как ограничить размер до распаковки и почему `Content-Length` тут врёт?

**54.** `httpx` и `aiohttp` по-разному ведут себя при отмене (`asyncio.CancelledError`) внутри запроса. Что происходит с соединением: возвращается в пул, закрывается, или остаётся в неопределённом состоянии? Почему это грязный источник багов с `asyncio.wait_for`?

**55.** `aiohttp` парсит HTTP через C-парсер `llhttp` (раньше `http-parser`), `httpx` — через чистый Python `h11`. Оцени разницу в CPU на 10k мелких ответов и объясни, зачем `httpx` осознанно выбрал медленнее.

**56.** `requests` построен на `urllib3`, у которого своя ретрай-логика, свой пул и свои таймауты. Назови три случая, когда параметры `requests` и `urllib3` конфликтуют и побеждает нижний слой.

**57.** В Go `http.Client` по умолчанию тоже без таймаута, но у транспорта есть `IdleConnTimeout` и `MaxIdleConnsPerHost`. Чего из этого нет в `requests` и как ты ловишь мёртвые keep-alive соединения после того, как балансировщик тихо порвал их?

**58.** `Connection reset by peer` на первом запросе после 60 сек простоя пула — классика. Почему keep-alive соединение «протухает» без уведомления, что такое `retry on idempotent request` в этом контексте, и почему `pool_recycle`-подход из SQLAlchemy тут применим?

**59.** Мокать HTTP: `responses` (патчит адаптер `requests`), `respx` (патчит транспорт `httpx`), `aioresponses`, `pytest-httpserver` (реальный сокет), VCR-кассеты. Разложи по оси «скорость vs правдоподобие» и скажи, какой класс багов каждый пропускает.

**60.** Ты пишешь SDK для внутреннего API, который дёргают и из sync-Django, и из async-FastAPI. Один клиент на `httpx` с двумя фасадами, два независимых клиента, или sync-обёртка через `asyncio.run`? Аргументируй, где ловушка с event loop и пулами.

---

**Бонус-киллер:** `timeout=10` в `requests` не гарантирует, что запрос завершится за 10 секунд, — при трёх редиректах и медленной отдаче он честно проработает минуты. Объясни, к чему привязан этот таймаут на уровне сокета, и придумай способ получить настоящий wall-clock deadline на весь запрос, не выходя из синхронного кода и не убивая поток.

Хочешь разбор ответов? Скажи номера.
