---
topic: asyncio-basics
block: "IV. Конкурентность"
number: 31
status: todo
---

# Собес Python — asyncio-basics (event loop, coroutine, Task, await, run)

## БАЗА

1. Что возвращает вызов `async def f()` без `await`? Какого типа объект и что с ним произошло на этот момент?
2. Чем корутина отличается от Task? Когда код корутины начинает реально исполняться в каждом из случаев?
3. Что делает `asyncio.run(coro)` по шагам? Почему его нельзя вызвать внутри уже работающего event loop?
4. Разница между `await coro` и `await asyncio.create_task(coro)` с точки зрения конкурентности — что выполнится параллельно, что последовательно?
5. Что такое awaitable? Перечисли три категории объектов, которые можно `await`-ить.
6. Что произойдёт при `time.sleep(5)` внутри корутины? Чем это отличается от `await asyncio.sleep(5)`?
7. Зачем `asyncio.gather` нужен, если можно просто последовательно await-ить каждую корутину в цикле?
8. Что такое Future? Чем Future отличается от Task в иерархии классов?
9. Что выведет warning `coroutine 'f' was never awaited` и в какой момент он появляется?
10. Как получить текущий event loop внутри корутины? Чем `asyncio.get_running_loop()` отличается от `asyncio.get_event_loop()`?
11. Может ли один event loop работать в двух потоках одновременно? Что произойдёт при попытке?
12. Что вернёт `asyncio.gather(a(), b(), return_exceptions=True)`, если `b()` бросит исключение?
13. Что такое `await` с точки зрения протокола — какой dunder-метод дёргается?
14. Сколько корутин выполняется в один момент времени в одном event loop? Почему asyncio называют конкурентностью, а не параллелизмом?

## СРЕДНИЙ УРОВЕНЬ

15. Корутина внутри реализована поверх генератора. Какой флаг в `co_flags` отличает `async def` от обычной функции и что делает `types.coroutine`?
16. `await` на awaitable, который никогда не yield-ит в loop (например `await asyncio.sleep(0)` vs `await some_ready_future()`) — в каком случае управление реально уходит в loop?
17. Что делает `asyncio.sleep(0)` и зачем его вставляют в CPU-bound циклы? Есть ли гарантия, что другие Task-и получат управление?
18. Task создан, но ссылку на него никто не сохранил. Что говорит документация про сборку мусора и почему `create_task` в fire-and-forget стиле — баг?
19. Ты бросил исключение внутри Task, но результат никто не await-ил. Когда и куда попадёт traceback? Что за `Task exception was never retrieved`?
20. Что делает `loop.call_soon` и в какой очереди оказывается callback? Чем отличается от `call_soon_threadsafe`?
21. Опиши один tick event loop-а: что делает `_run_once` — как считается таймаут селектора и в каком порядке разбираются ready-очередь и таймеры.
22. `asyncio.run` создаёт новый loop и закрывает его. Что происходит с pending-тасками в момент закрытия и что делает `_cancel_all_tasks`?
23. `asyncio.Task.cancel()` — синхронно ли отменяет? Что реально происходит внутри и когда таск станет `cancelled()`?
24. Как поймать `CancelledError` и НЕ проглотить отмену? Почему в 3.8 `CancelledError` переехал из `Exception` в `BaseException`?
25. Чем `asyncio.wait_for(coro, timeout)` отличается от `asyncio.timeout()` (3.11)? Что происходит с корутиной по истечении таймаута в каждом случае?
26. Какой ивент-луп используется по умолчанию на Windows в 3.8+ и почему сменили? Что ломается при `ProactorEventLoop` vs `SelectorEventLoop`?
27. Как запустить блокирующий вызов из корутины, не убив loop? Чем `asyncio.to_thread` отличается от `loop.run_in_executor` и сколько потоков в дефолтном executor?
28. Что делает `loop.set_debug(True)` / `PYTHONASYNCIODEBUG=1`? Какие проверки включаются и на что жалуется `slow callback`?
29. `gather` vs `asyncio.wait` vs TaskGroup (3.11) — три разных семантики отмены при первой ошибке. Опиши каждую.
30. Почему `asyncio.Queue` не потокобезопасна, хотя выглядит как `queue.Queue`? Что в ней вообще нет синхронизации?
31. Что вернёт `asyncio.current_task()` в callback, переданном в `call_soon`? Почему?

## ЗАДАЧИ (напиши код)

32. Напиши async-функцию `fetch_all(urls, limit)`, которая качает все URL конкурентно, но не более `limit` одновременно. Без сторонних либ, только asyncio-примитивы. Какая сложность по памяти при 1M URL?
33. Реализуй `async def race(*coros)` — возвращает результат первой завершившейся корутины, остальные корректно отменяет и дожидается их отмены. Что будет, если первая завершилась исключением?
34. Напиши свой класс, который можно `await`-ить: `await MyTimer(1.0)` спит секунду и возвращает реальное затраченное время. Через `__await__`, без наследования от Future.
35. Реализуй `async def retry(coro_factory, attempts, delay)` с экспоненциальным backoff. Почему на вход именно фабрика, а не сама корутина?
36. Напиши async-функцию `gather_with_timeout(coros, timeout)`: вернуть результаты завершившихся за `timeout`, остальные отменить, вернув `None` на их местах. Порядок результатов должен совпадать с порядком входа.
37. Реализуй минимальный event loop на голых генераторах: `sleep(t)` и `run(tasks)`, без `asyncio`. Достаточно круглого списка + heapq по времени. Где твоя реализация проигрывает CPython-овской?
38. Напиши async context manager `TaskPool`, который на выходе гарантированно дожидается или отменяет все запущенные внутри таски. Сравни с `asyncio.TaskGroup` — чего у тебя не хватает?

---

# 20 ВОПРОСОВ НА СЕНЬОРНОСТЬ

**39.** PEP 492 ввёл `async`/`await` в 3.5, но корутина всё ещё под капотом генератор. Какие именно поля `PyCoroObject` отличаются от `PyGenObject` и зачем нужен `cr_origin`?

**40.** До PEP 492 писали `yield from`, после — `await`. Почему нельзя было просто оставить `yield from` и что сломалось бы при `await` внутри обычного генератора-итератора?

**41.** `asyncio.sleep(0)` в CPython реализован через отдельную ветку — не создаёт таймер, а делает `__sleep0()`. Что она yield-ит и почему это дешевле, чем `sleep(0.0001)` на 3 порядка?

**42.** Дефолтный `switch` в asyncio отсутствует: корутина без `await` захватывает loop навсегда. Как задетектить такую голодовку в проде без изменения кода — что даёт `loop.slow_callback_duration` и почему в нём дефолт 0.1?

**43.** `uvloop` даёт ~2-4x на echo-сервере против стандартного loop. За счёт чего именно — libuv, Cython, отсутствие `Handle`? Какой ценой (что перестаёт работать)?

**44.** В Go горутина при блокирующем syscall уезжает на другой M-поток и остальные работают. В Python один `time.sleep` в корутине вешает весь loop. Почему asyncio не может сделать так же — что мешает на уровне рантайма?

**45.** PEP 3156 (2012) выбрал явные `await` вместо неявных green threads (gevent). Какую цену заплатили («function coloring») и какой аргумент был решающим за explicit?

**46.** Task, который отменяют в момент, когда его корутина внутри `finally` делает `await cleanup()`. Что произойдёт? Как защититься и почему `asyncio.shield` — не полное решение?

**47.** `asyncio.CancelledError` унаследовали от `BaseException` в 3.8 (bpo-32528). Какой класс багов это закрыло и какой открыло в коде, писавшем `except Exception`?

**48.** TaskGroup (3.11) при ошибке одного таска отменяет остальных и кидает `ExceptionGroup` (PEP 654). Почему нельзя было просто кинуть первое исключение, как делает `gather`? Какой реальный сценарий это чинит?

**49.** `wait_for` до 3.8 имел race: таймаут срабатывал одновременно с успешным завершением, результат терялся. Опиши гонку по шагам и как её закрыли через `uncancel()` (3.11).

**50.** У тебя 500k одновременных TCP-соединений, 32GB RAM. Task на соединение = сколько байт минимум? Что съедает память в asyncio на такой нагрузке и какая архитектура вместо «таск на коннект»?

**51.** Event loop использует `selectors.DefaultSelector` → epoll на Linux. Почему epoll в edge-triggered режиме asyncio НЕ использует и что было бы выигрышем/риском?

**52.** `create_task` не держит сильную ссылку на Task — только weakref в `_all_tasks`. Почему это осознанное решение, а не баг? Какой альтернативный дизайн отвергли и почему он течёт?

**53.** Вызов `asyncio.run` внутри библиотеки — почему это считается антипаттерном? Опиши конкретный сценарий, где два `asyncio.run` в одном процессе порождают невоспроизводимый баг с `ThreadPoolExecutor`.

**54.** `await` на Future из другого event loop. Что произойдёт — исключение, дедлок, тихая порча? Какая проверка внутри `Task.__step` и что делает `run_coroutine_threadsafe`?

**55.** DoS-угол: клиент открывает соединение и шлёт по 1 байту в час. Твой `await reader.readline()` висит вечно. Сколько таких коннектов положит сервер и какие три уровня защиты ты поставишь?

**56.** В 3.12 `asyncio.get_event_loop()` без running loop даёт `DeprecationWarning`, в 3.14 — ошибку. Что за многолетний класс багов этим лечат и почему на это ушло 6 версий?

**57.** Контекст: `contextvars` (PEP 567) и Task. Каждый Task копирует контекст при создании — в какой момент и через что? Почему `threading.local` в asyncio не работает и что было до 3.7?

**58.** Trade-off без правильного ответа: 200 RPS, каждый запрос = 3 IO-вызова по 50 мс + 5 мс CPU. asyncio один процесс vs потоки vs 4 процесса с asyncio. Что выберешь и какой замер решит спор?

---

**Бонус-киллер:** Корутина `async def f(): return 42` — при `await f()` управление в event loop не уходит ни разу, планировщик не вызывается, `f` исполняется синхронно прямо в стеке вызывающего. Тогда чем `await f()` отличается от `f()` без `async`, кроме синтаксиса — и почему тысяча таких «синхронных» await-ов подряд всё-таки медленнее обычных вызовов на порядок?

Хочешь разбор ответов? Скажи номера.
