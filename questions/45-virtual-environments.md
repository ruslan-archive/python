---
topic: virtual-environments
block: "VI. Модули, пакеты, окружение"
number: 45
status: todo
---

# Собес Python — VIRTUAL ENVIRONMENTS

## БАЗА

1. Что такое виртуальное окружение и какую проблему оно решает? Что физически лежит внутри папки `.venv`?

2. Чем `venv` отличается от `virtualenv`? Почему `venv` попал в stdlib и с какой версии?

3. Что делает скрипт `activate`? Почему его надо запускать через `source`, а не как обычный скрипт?

4. Активация окружения — обязательна? Как запустить код в окружении без `activate`?

5. Что такое `site-packages`? Чем отличаются `purelib` и `platlib`?

6. Разница между `sys.path`, `PYTHONPATH` и `site-packages` — что откуда берётся и в каком порядке?

7. Что делает флаг `--system-site-packages` при создании venv? Когда он полезен, когда опасен?

8. Зачем нужен `pyvenv.cfg`? Какие ключи в нём бывают?

9. Что такое `sys.prefix` и `sys.base_prefix`? Как по ним понять, что код исполняется внутри venv?

10. Почему venv нельзя просто переместить в другую директорию или переименовать?

11. Чем `pip install -e .` отличается от `pip install .` с точки зрения того, что появляется в `site-packages`?

12. Что такое `user site-packages` (`~/.local/lib/python3.x/site-packages`)? Как флаг `--user` взаимодействует с активным venv?

13. Зачем нужен `python -m pip` вместо просто `pip`? Какую конкретную ошибку это предотвращает?

## СРЕДНИЙ УРОВЕНЬ

14. Опиши полную последовательность инициализации `sys.path` при старте интерпретатора: кто, в каком порядке и на основании чего его собирает.

15. Что делает модуль `site` на старте? Что произойдёт при запуске с `-S`?

16. Как работают `.pth`-файлы? Почему строка, начинающаяся с `import `, в `.pth` исполняется — и почему это дыра в безопасности?

17. PEP 405 ввёл venv. Какой механизм обнаружения prefix он описывает и чем `pyvenv.cfg` заменил старый подход `virtualenv` с копированием бинарников?

18. В Python 3.11 переписали инициализацию путей (PEP 587 / `_PyPathConfig`). Что поменялось в поведении `sys._base_executable` и почему это сломало часть инструментов?

19. Symlink vs copy интерпретатора в venv: `--copies`, `--symlinks`. На каких платформах какой дефолт и почему?

20. PEP 668 (`EXTERNALLY-MANAGED`). Что именно он запрещает, кто его кладёт и как его корректно обойти в Docker?

21. Как shebang в `.venv/bin/pip` соотносится с лимитом длины shebang в 127 символов на Linux? Что делает `pip` при слишком длинном пути?

22. Что такое `sysconfig` scheme? Как `pip` вычисляет, куда именно ставить файлы пакета?

23. Порядок в `sys.path`: почему пустая строка / cwd в начале — это проблема? Что изменил PEP 741 / флаг `-P` и `PYTHONSAFEPATH`?

24. Как venv взаимодействует с `PYTHONHOME`? Почему установленный `PYTHONHOME` ломает активированное окружение?

25. `pip freeze` внутри venv с `--system-site-packages` — что попадёт в вывод и почему это ловушка для reproducible builds?

26. Что делает `python -m venv --upgrade`? Что он НЕ обновляет и почему после апгрейда minor-версии Python окружение часто мертво?

27. `importlib.metadata.distributions()` — откуда он читает список пакетов? Что произойдёт, если руками удалить папку пакета, но оставить `.dist-info`?

28. `ensurepip` — как он ставит pip без pip? Что делает `--without-pip` и зачем это нужно в CI?

## ЗАДАЧИ (напиши код)

29. Напиши функцию `in_venv() -> bool`, которая надёжно определяет, исполняется ли код внутри venv. Учти и `venv`, и `virtualenv`, и `conda`. Какие есть false positive?

30. Напиши CLI-скрипт, который принимает путь к venv и печатает: версию Python, список пакетов с версиями, флаг system-site-packages. Не активируя окружение и не импортируя ничего из него.

31. Реализуй функцию `resolve_import(name: str, paths: list[str]) -> str | None`, повторяющую порядок поиска модуля по `sys.path` (пакет vs модуль, `__init__.py`, приоритет). Какая сложность? Где твоя реализация разойдётся с реальным `FileFinder`?

32. Напиши context manager `temp_syspath(*paths)`, который временно добавляет пути в `sys.path`. Что нужно сделать с `sys.path_importer_cache` и `importlib.invalidate_caches()`, чтобы не словить stale-кэш?

33. Напиши функцию, которая сравнивает два `requirements.txt` и печатает diff: added / removed / version-changed. Учти extras, маркеры окружения и `-e` строки.

34. Есть venv, скопированный с чужой машины (пути внутри битые). Напиши скрипт, который его чинит: перепишет `pyvenv.cfg`, shebang'и всех скриптов в `bin/` и симлинки. Какие случаи починить невозможно?

35. Реализуй `find_shadowed(name: str)` — вернуть ВСЕ места в `sys.path`, где лежит модуль с таким именем, в порядке приоритета. Зачем это нужно на проде?

---

# 20 ВОПРОСОВ НА СЕНЬОРНОСТЬ

**36.** Ты сделал `pip install -e .` в venv. В `site-packages` появился `__editable__*.pth` вместо старого `egg-link`. PEP 660 описывает механизм. Как editable-install через import hook (`__editable___*_finder.py`) ломает `pkgutil.iter_modules()` и namespace packages?

**37.** `venv` копирует `python` бинарник на Windows и симлинчит на Linux. На macOS с framework-сборкой Python симлинк на `python3` даёт `sys.prefix`, указывающий в system Python. Почему? Что чинит `--copies` и при чём тут `dyld` и `@executable_path`?

**38.** В контейнере `python:3.12-slim` ты ставишь зависимости в venv на builder-стадии и копируешь `.venv` в runtime-стадию через `COPY --from`. Что сломается, если базовый образ runtime отличается glibc-версией? Почему `manylinux` wheel это не спасает?

**39.** `sys.path` внутри venv не содержит `site-packages` базового Python — но содержит stdlib. Кто и на каком шаге исключает базовый `site-packages`, и что именно проверяет `site.py` в `pyvenv.cfg`, чтобы принять решение?

**40.** У тебя `PYTHONPATH=/opt/legacy` и активированный venv. Пакет `requests` есть в обоих местах. Какой выиграет и почему? Теперь ты добавил `/opt/legacy/foo.pth` — меняется ли ответ? Обоснуй через порядок работы `site.addsitedir()`.

**41.** Namespace packages (PEP 420) + venv: пакет `acme.foo` в venv, `acme.bar` в `PYTHONPATH`. Собрались в один namespace? Что произойдёт, если в одной из директорий появится `acme/__init__.py`? Почему это самый неприятный класс багов в монорепо?

**42.** Атака: злоумышленник кладёт `.pth`-файл в `site-packages` через зависимость с `setup.py`. Файл содержит `import os; os.system(...)`. Когда исполнится код? Почему `pip install --no-deps` не спасает, а wheel-only install — частично спасает?

**43.** `pip install` в venv за 40 секунд, `uv pip install` — за 0.8. Разбери, откуда 50x: hardlink вместо copy, global cache, отсутствие subprocess-вызовов `setup.py`, resolver на PubGrub. Что из этого — фундаментально, а что pip мог бы догнать?

**44.** PEP 582 (`__pypackages__`, local packages без venv) был отклонён. Какие конкретные аргументы убили его? Почему Node с `node_modules` живёт с этим нормально, а Python — нет? (Подумай про `sys.path` и про то, кто резолвит зависимости.)

**45.** Ты запускаешь `pytest` из venv, он импортирует твой пакет из cwd, а не из установленной версии. Разбери роль `rootdir`, `conftest.py`, `importmode=prepend|importlib` и того самого пустого элемента в `sys.path`. Почему `src/`-layout решает проблему, а flat layout — нет?

**46.** Внутри venv `pip` — это скрипт с shebang на абсолютный путь. Ты примонтировал venv в контейнер по другому пути. `pip` умер, но `python -m pip` работает. Объясни разницу через механизм запуска и через `console_scripts` entry points.

**47.** `python -c "import sys; print(sys.path)"` в venv показывает stdlib-путь базового Python. Значит, ты можешь снести venv, но не базовый Python. Как в этом свете работает `--copies` на Linux и почему он всё равно не делает venv self-contained? Что предлагает PEP 711 / relocatable-подход?

**48.** У тебя 300 микросервисов, каждый со своим venv, суммарно 40GB на CI-раннере. Твоя архитектура кэширования? Разбери trade-off: shared wheel cache vs hardlink-farm (`uv`) vs один жирный venv с constraints. Что ломает hardlink-подход?

**49.** `conda` и `venv` в одном шелле: `conda activate` потом `source .venv/bin/activate`. Что стало с `PATH`, `sys.prefix` и с C-библиотеками (`libssl`, `libstdc++`)? Почему это специфический ад именно для пакетов с бинарными расширениями?

**50.** В Go — статический бинарник, в Rust — cargo с lockfile и таргет-директорией, в Java — classpath/jar. Ни у кого нет «активации окружения». Что в модели импорта CPython делает venv неизбежным? Мог бы `sys.path`-подход быть заменён на что-то ещё без слома обратной совместимости?

**51.** Ты обновил Python 3.11 → 3.12 на хосте. Все venv, созданные симлинком, умерли с `ModuleNotFoundError: encodings`. Что именно сломалось на уровне path config? Почему `python -m venv --upgrade` иногда чинит, а иногда нет, и что делать с уже скомпилированными C-extension в этом venv?

**52.** `pip install package` в venv, где `package` уже установлен другой версией через `--system-site-packages` базового Python. Что произойдёт при `pip uninstall`? Trade-off: изоляция полная vs `--system-site-packages` ради тяжёлых пакетов (torch, numpy с MKL). Когда ты сознательно ломаешь изоляцию и как страхуешься?

---

**Бонус-киллер:** Ты создал venv, активировал, поставил пакет. Затем удалил `.venv/bin/python` (симлинк), но оставил всё остальное. Уже запущенный процесс продолжает работать и корректно импортирует пакеты. Новый `python -m venv --upgrade` тоже отрабатывает. Почему первое работает — и что именно во втором случае восстанавливает симлинк, если `pyvenv.cfg` указывает на базовый Python, которого ты тоже уже успел обновить?

Хочешь разбор ответов? Скажи номера.
