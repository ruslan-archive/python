---
topic: attributes-mro-descriptors
block: "Под капотом"
number: 5
status: todo
---

# Собес Python — Атрибуты, MRO, дескрипторы, метаклассы

## БАЗА

**1.** Опиши полный порядок поиска атрибута при `obj.x`: где в цепочке `__getattribute__`, data-дескриптор, `instance.__dict__`, non-data дескриптор, `__getattr__`?

**2.** Чем `__getattr__` отличается от `__getattribute__`? Когда вызывается каждый?

**3.** Что такое дескриптор? Какие три метода образуют протокол?

**4.** В чём разница между data-дескриптором и non-data дескриптором? Как класс попадает в одну из категорий?

**5.** Почему обычная функция, доступная как `instance.method`, автоматически становится связанным методом (bound method)? При чём тут дескрипторы?

**6.** Что такое MRO? Что показывает `Cls.__mro__` и `Cls.mro()`?

**7.** Для чего нужен `super()`? Почему говорят, что он идёт «по MRO», а не «к родителю»?

**8.** Что делает `__slots__`? Какой атрибут у инстанса пропадает при его объявлении?

**9.** Что такое метакласс? Кто метакласс у обычного класса по умолчанию?

**10.** `type` вызывают двумя способами: `type(obj)` и `type(name, bases, ns)`. Опиши разницу.

**11.** В чём разница между `@staticmethod`, `@classmethod` и обычным методом с точки зрения того, что приходит первым аргументом?

**12.** Что делает `property`? Как через неё сделать атрибут read-only?

**13.** Почему `AttributeError` внутри `property.__get__` может «замаскироваться» под отсутствие атрибута и уйти в `__getattr__`?

## СРЕДНИЙ УРОВЕНЬ

**14.** Алгоритм C3-линеаризации: сформулируй правило, по которому строится MRO при множественном наследовании, и что такое «монотонность».

**15.** Приведи ромб наследования, для которого C3 бросит `TypeError: cannot create a consistent method resolution order`. Почему невозможно построить линеаризацию?

**16.** PEP 487 добавил `__init_subclass__`. В какой момент он вызывается, на каком классе, и что приходит в `cls`?

**17.** PEP 487 добавил `__set_name__`. Кто и когда его вызывает у дескриптора, какие аргументы передаёт?

**18.** Почему до PEP 487 дескриптору, чтобы узнать имя атрибута, под которым его положили в класс, требовался метакласс или явное имя в конструкторе?

**19.** Опиши поток `type.__call__` при `MyClass(args)`: в каком порядке дёргаются `__new__`, `__init__` и чей `__call__` тут работает.

**20.** Как перекрытие `__getattribute__` в подклассе взаимодействует с data-дескрипторами родителя? Что имеет приоритет?

**21.** `__slots__` объявлен, но один из подклассов не объявляет `__slots__`. Что происходит с `__dict__` у инстансов подкласса и с экономией памяти?

**22.** Дескриптор — атрибут КЛАССА. Что произойдёт, если положить экземпляр дескриптора в `instance.__dict__` напрямую? Сработает ли `__get__`?

**23.** Разница `metaclass.__new__` vs `metaclass.__init__`: что можно сделать в каждом, почему изменение `namespace` обычно делают в `__new__`?

**24.** Как связаны `__set_name__` и порядок объявления атрибутов? В каком порядке будут вызваны `__set_name__` для нескольких дескрипторов в одном классе?

**25.** Почему `super().__init__()` в кооперативном наследовании требует, чтобы ВСЕ классы в цепочке вызывали `super()`? Что ломается, если один не вызовет?

**26.** Perf: во сколько раз примерно `__slots__` может ускорить доступ к атрибуту и сколько байт на инстанс экономит по сравнению с `__dict__`? Откуда экономия.

**27.** Конфликт метаклассов: два родителя с разными метаклассами. Какое правило Python применяет к метаклассу результирующего класса и когда бросает `TypeError`?

**28.** `vars(obj)`, `obj.__dict__`, `dir(obj)` — что каждый вернёт для инстанса со `__slots__`? Где увидишь слот-атрибуты?

## ЗАДАЧИ (напиши код)

**29.** Напиши data-дескриптор `Typed`, валидирующий тип при `__set__` (бросает `TypeError`), с автоопределением имени через `__set_name__`. Укажи сложность доступа. Покажи второй способ — без `__set_name__`, через явное имя в `__init__`.

**30.** Реализуй `cached_property` вручную (non-data дескриптор): первый доступ считает и кладёт результат в `instance.__dict__`, дальнейшие берут оттуда. Объясни, почему это работает только для non-data дескриптора. Второй способ — через `__get__` + флаг.

**31.** Дан ромб A→B,C→D. Напиши классы так, чтобы `D.mro()` был `[D, B, C, A, object]`, и метод в каждом печатал своё имя через кооперативный `super()`. Приведи вывод. Второй способ — покажи, как перестановка баз меняет MRO.

**32.** Напиши метакласс `AutoRepr`, который автоматически генерирует `__repr__` из аннотаций класса. Оцени, где дешевле — метакласс или `__init_subclass__`. Реализуй оба способа.

**33.** Реализуй класс `Frozen` (иммутабельный после `__init__`): любой `__setattr__` после инициализации → `AttributeError`. Оцени сложность. Второй способ — через `__slots__` + дескриптор.

**34.** Напиши мини-ORM поле `Field` как дескриптор с валидацией + метакласс `ModelMeta`, собирающий все поля в `cls._fields`. Второй способ сбора полей — через `__init_subclass__` вместо метакласса. Сравни.

---

# 20 ВОПРОСОВ НА СЕНЬОРНОСТЬ

**35.** В CPython доступ к атрибуту проходит через слот `tp_getattro` в `PyTypeObject`. Чем отличаются `tp_getattro` и `tp_getattr` (устаревший), и как `object.__getattribute__` в этом слоте реализует приоритет data-дескрипторов через `_PyObject_GenericGetAttrWithDict`?

**36.** C3-линеаризация: докажи, что алгоритм монотонен и сохраняет порядок локального предшествования (local precedence order). Приведи конфигурацию, где наивный DFS/BFS дал бы MRO, нарушающий монотонность, а C3 — корректный.

**37.** PEP 487 заменил целые метаклассы на `__init_subclass__`/`__set_name__`. Оцени цену: какие кейсы (например, вмешательство в `namespace` до создания класса, изменение `bases`) PEP 487 закрыть НЕ может и метакласс остаётся единственным вариантом?

**38.** Паттерн что ломается: атрибут в `instance.__dict__` совпадает по имени с non-data дескриптором класса. Покажи, как замена дескриптора с non-data на data (добавили `__set__`) молча ломает поведение существующего кода. Почему `cached_property` уязвим тут?

**39.** `__slots__` и память: для класса с 3 int-атрибутами оцени в байтах размер инстанса со `__slots__` vs с `__dict__` (CPython 3.11+, с учётом split-table dict и PEP 659). Откуда берётся разница и почему split-dict уже сильно сократил разрыв?

**40.** Security: `__getattribute__`/`__getattr__` — вектор обхода «песочницы». Как через `type(obj).__subclasses__()` или dunder-обход добраться до опасных объектов, и почему фильтрация по именам атрибутов на уровне Python принципиально дырява?

**41.** Архитектурный сценарий: валидационный слой на дескрипторах для 200 полей в 50 моделях. Где дескрипторы бьют по перфу vs `__init_subclass__`-валидация, и когда осознанно выберешь генерацию `__init__` кодом (attrs/dataclass) вместо дескрипторов?

**42.** Кросс-язык: сравни Python-дескрипторы с Java-полями + reflection и с C# properties. Почему в Python property — это объект в `__dict__` класса, а не синтаксический сахар компилятора, и что это даёт в рантайме?

**43.** Кросс-язык: Ruby `method_missing` vs Python `__getattr__`. В чём разница момента срабатывания (Ruby — при отсутствии метода в цепочке, Python — при `AttributeError` из `__getattribute__`) и почему Python-подход дороже для «прокси»-объектов?

**44.** История py2: old-style classes не имели `__getattribute__` и полноценного дескриптор-протокола на инстансах. Что именно ломалось для property/`super()` в old-style, и почему `object` как базовый класс стал обязателен для дескрипторов?

**45.** История py2: `__metaclass__` как атрибут класса/модуля vs `class C(metaclass=M)` в py3. Почему модульный `__metaclass__` был опасен и как py3 сделал разрешение метакласса детерминированным?

**46.** Trade-off: метакласс vs декоратор класса для одной задачи (регистрация классов в реестре). Назови три различия по семантике наследования (декоратор не наследуется, метакласс — да) и по моменту выполнения.

**47.** `super()` без аргументов в py3 опирается на `__class__` cell и скрытый аргумент компилятора. Что лежит в этой ячейке, почему `super()` не работает вне тела метода, и как это связано с `__classcell__` в namespace, передаваемом метаклассу?

**48.** `type.__call__` vs `__new__` метакласса: разложи, что вызывается при СОЗДАНИИ класса (`class` statement → `metaclass.__call__` → `metaclass.__new__/__init__`) vs при создании ИНСТАНСА (`Class()` → `type(Class).__call__`). Где тут два разных `__call__`?

**49.** Data-дескриптор с тяжёлым `__set__` в горячем цикле. Как это профилировать, и почему замена дескриптора на `__slots__`-слот (тоже дескриптор `member_descriptor`, но C-level) даёт другой профиль по `tp_descr_get`?

**50.** MRO и `__mro_entries__` (PEP 560): как объекты, не являющиеся классами (например, `Generic[T]`), участвуют в построении баз до вызова C3? Что происходит между `class` statement и `type.__new__`?

**51.** `__slots__` в CPython создаёт `member_descriptor` со смещением в структуре инстанса (`tp_members`, `offset`). Объясни, как это даёт O(1) доступ без хеширования и почему наследование от двух классов с непустыми `__slots__` может дать `TypeError: multiple bases have instance lay-out conflict`.

**52.** Метаклассы и `__prepare__` (PEP 3115): зачем нужен `__prepare__`, какой объект он возвращает и как это позволяет, например, `enum` запоминать порядок и запрещать дубликаты имён ДО создания класса?

**53.** Кооперативный `super()` в diamond-иерархии с разными сигнатурами методов. Почему это классический источник багов, как `**kwargs`-протокол «съедания» аргументов вдоль MRO решает проблему, и где он всё равно ломается?

**54.** `__getattribute__` вызывается на КАЖДЫЙ доступ к атрибуту, включая доступ изнутри самого `__getattribute__`. Как избежать бесконечной рекурсии корректно (`object.__getattribute__` / `super().__getattribute__`), и почему `self.__dict__[name]` внутри — тоже ловушка?

---

**Бонус-киллер:** Класс `A` со `__slots__ = ('x',)` наследуется `B`, где `x` переопределён как `property`. Инстанс `B().x = 5` — что произойдёт: сработает слот-дескриптор `A`, property `B`, или `AttributeError`? Объясни через порядок дескрипторов в MRO и приоритет data-дескрипторов при перекрытии одноимённых атрибутов в разных классах цепочки.

Хочешь разбор ответов? Скажи номера.
